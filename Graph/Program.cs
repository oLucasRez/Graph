using System;

namespace Graph
{
    class Program
    {
        static void Main(string[] args)
        {
            UndirectedGraph<char> graph = new  UndirectedGraph<char>();
            graph.AddNode('a');
            graph.AddNode('b');
            graph.AddNode('c');
            graph.AddNode('a');
            graph.AddNode('e');
            graph.AddNode('g');
            graph.AddNode('f');
            graph.AddNode('d');
            graph.AddEdge
        }
    }
}
